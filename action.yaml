name: 'GitHub Action for Firebase with Java'
author: 'OC Fulfillment Team'
description: 'Wraps the firebase-tools CLI to enable common commands together with Java in order to start the firebase emulator.'
branding:
  icon: 'package'
  color: 'gray-dark'
runs:
  using: 'docker'
  image: 'Dockerfile'
